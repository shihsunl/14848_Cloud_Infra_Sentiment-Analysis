#!/bin/bash

# downloads all the Javascript dependencies of the React application
npm install
# build application into static files. Generates build folder containing all the static files ReactJS application
npm run build
